<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/frameLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FBFAF7"
    tools:context=".presentation.map.MapFragment">

    <TextView
        android:id="@+id/tv_book_map"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="15dp"
        android:layout_marginStart="15dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/pretendard_semibold"
        android:text="Book Map"
        android:textColor="@color/black"
        android:textSize="30dp" />

    <EditText
        android:id="@+id/et_home_search"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_book_map"
        android:layout_marginTop="10dp"
        android:layout_width="330dp"
        android:layout_height="45dp"
        android:background="@drawable/ic_map_search_round_box"
        android:hint="검색할 공간을 입력하세요"
        android:paddingStart="15dp"
        android:textSize="14sp"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:cursorVisible="false"/>

    <ImageView
        android:id="@+id/iv_home_search_ic"
        app:layout_constraintBottom_toBottomOf="@id/et_home_search"
        app:layout_constraintEnd_toEndOf="@id/et_home_search"
        app:layout_constraintTop_toTopOf="@id/et_home_search"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginEnd="10dp"
        android:src="@drawable/ic_home_search" />

</androidx.constraintlayout.widget.ConstraintLayout>